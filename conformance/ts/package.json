{
  "name": "pwrap-conformance-ts",
  "private": true,
  "type": "module",
  "description": "Cross-SDK conformance runner for @pwrap/sdk.",
  "scripts": {
    "start": "tsx runner.ts",
    "start:no-global-ws": "tsx --import ./no-global-ws.mjs runner.ts"
  },
  "//": "Depends on the built package rather than src/, so the runner exercises the exports map and type declarations a real consumer gets. Run `pnpm --filter @pwrap/sdk build` first.",
  "dependencies": {
    "@pwrap/sdk": "file:../../sdk/ts",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.4.0",
    "@types/ws": "^8.5.13"
  }
}
