// Mirrors sdk/go/pwrap/{client,vector}.go. Keep in sync with the Go SDK so the two
// agree on what a "v0" tenant looks like.
export const SchemaVersion = "0001_init" as const;
export const VectorDim = 1536 as const;
